# Security Policy

SSHEasy 是一个会接触用户 **SSH 凭据** 的桌面客户端，安全是本项目的第一优先级。本文档说明凭据的处理方式、已知的安全边界，以及如何报告漏洞。

## 支持的平台

- **目前仅支持 macOS。** 密码与私钥 passphrase 依赖 **macOS Keychain**（`security-framework`）存储，密钥加载依赖系统 `openssl`。Windows / Linux 的凭据后端尚未实现。
- 仅 macOS 经过了实际安全行为验证。

## 凭据存储

| 数据 | 存储位置 | 说明 |
| --- | --- | --- |
| 服务器密码 | macOS Keychain | 不进入数据库 |
| 私钥 passphrase | macOS Keychain | 不进入数据库 |
| 私钥文件本身 | 用户自管路径（如 `~/.ssh/`） | 仅存**文件路径**，不复制、不上传 |
| host / port / user / 认证方式 | 本地 SQLite | **不含任何秘密** |

数据库（`*.db`）只保存连接所需的非敏感元数据；即便数据库文件泄露，攻击者也拿不到任何可直连的凭据。

## 私钥处理

- **未加密私钥**：russh 直接加载，全程在内存。
- **OpenSSH / PKCS#8 加密私钥**：russh 直接加载（用 Keychain 中的 passphrase）。
- **OpenSSL 老式加密 PEM**（`Proc-Type: 4,ENCRYPTED`）：调用**系统 `openssl`** 解密。
  - 解密输出捕获到**内存**（stdout），失败时**完全不落盘**。
  - 由于 russh 0.49.2 的密钥加载 API 仅接受文件路径，解密后的明文 PEM 会短暂写入一个 **`0o600` 临时文件**（创建时即指定权限，无 `0o644` 中间态），供 russh 加载；加载完成后**立即清零文件内容并删除**。
  - **已知边界**：从 `load_secret_key` 调用到文件删除之间，磁盘上存在一个 `0o600` 的明文窗口。要做到「彻底不落盘」需升级 russh 以支持内存加载——这是已记录的后续改进项，但会触及连接层，需谨慎评估。

## 主机密钥校验（防中间人）

- 基于 **known_hosts**：首次连接某主机时，会弹出指纹确认；用户确认后指纹写入本地 known_hosts。
- **指纹变更检测**：若已记录的指纹与服务器当前指纹不一致，会告警并阻止自动连接，需用户显式确认，避免静默的中间人攻击。
- 未做校验的连接不会被悄悄建立。

## 操作审计

- 连接建立、命令执行、文件上传 / 下载 / 删除 / 重命名等动作均记录到本地日志，可在「日志」面板回看，便于追溯「谁在何时对哪台机器做了什么」。

## 传输与遥测

- 本应用**不会**向任何第三方发送凭据、命令内容或文件数据。
- 当前版本**不包含**任何遥测 / 网络上报逻辑；所有 SSH 流量只发生在你与目标服务器之间。

## 报告漏洞

如果你发现了安全漏洞，请**不要公开提 Issue**，而是通过以下方式私下告知：

- 邮箱：**security@ssheasy.dev**（如不可用，请在 GitHub 发起一个不公开细节的 Issue 并留下联系方式）
- 请尽量提供：复现步骤、受影响版本、潜在影响。

我们会在收到报告后 **72 小时内** 确认，并在修复后公开致谢（如你愿意）。

## 构建与验证（开发者）

```bash
npm install
npm run tauri dev
```

安全相关修改请额外注意：

- 不向数据库写入任何秘密；
- 私钥明文不长期驻留内存或磁盘；
- 任何新增的网络请求都应在 PR 描述中明确用途与目的地。
